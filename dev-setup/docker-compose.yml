services:
  build:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./hello-world:/project
    working_dir: /project

  build-img-receiver:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./img-receiver:/project
    working_dir: /project

  build-sassy-octopus:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./sassy-octopus:/project
      - ./hello-world/lib:/project/lib
    working_dir: /project

  build-supportive-octopus:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./supportive-octopus:/project
      - ./hello-world/lib:/project/lib
    working_dir: /project

  build-angry-octopus:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./angry-octopus:/project
      - ./hello-world/lib:/project/lib
    working_dir: /project

  build-conspiratorial-octopus:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./conspiratorial-octopus:/project
      - ./hello-world/lib:/project/lib
    working_dir: /project

  build-sad-octopus:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./sad-octopus:/project
      - ./hello-world/lib:/project/lib
    working_dir: /project

  build-chaotic-octopus:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./chaotic-octopus:/project
      - ./hello-world/lib:/project/lib
    working_dir: /project

  build-hungry-octopus:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./hungry-octopus:/project
      - ./hello-world/lib:/project/lib
    working_dir: /project

  build-tired-octopus:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./tired-octopus:/project
      - ./hello-world/lib:/project/lib
    working_dir: /project

  build-slaphappy-octopus:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./slaphappy-octopus:/project
      - ./hello-world/lib:/project/lib
    working_dir: /project
